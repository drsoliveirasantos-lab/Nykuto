# Opportunity Census change

Implemented on branch `feat/opportunity-census`.

The new census is intentionally decoupled from sequential execution so historical research can measure every distinct qualified setup, including setups that occur while another plan is active. It does not impose a daily trade limit.

The sequential engine remains the reference for realistic executability, risk, exposure and Lucid/session constraints.
